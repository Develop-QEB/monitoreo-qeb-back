# Render pone NODE_ENV=production por default, lo que hace que npm skipee
# devDependencies. Esta linea fuerza a instalarlas SIEMPRE — necesarias
# para el build (tsc + prisma generate + @types/*).
production=false
